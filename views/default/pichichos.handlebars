

<!-- Wrapper -->
<div id="wrapper" class="wrapper">



    <!-- Start Breadcaump Area -->
    <div class="breadcaump-area pt--130 pb--50 bg_color--1 breadcaump-title-bar">
        <div class="container">
            <div class="row">
                <div class="col-lg-12">
                    <div class="breadcaump-inner text-center">
                        <h1 class="heading heading-h1 pt-5 ">Pichichos buscando tu ayuda</h1>
                        
                    </div>
                </div>
            </div>
        </div>
    </div>
    <!-- End Breadcaump Area -->

    <!-- Page Conttent -->
    <main class="page-content">

        <!-- Start Portfolio Area -->
        <div class="brook-portfolio-area bg_color--1">
            <div class="container container-fluid content-row">
                {{#if adoption}}
                <div class="row">

                    <!-- Start Single Portfolio -->
                    {{#each adoption}}
                    <div class="col-lg-4 col-sm-6 col-12 move-up wow mt-4 mb-1">
                        <div class="portfolio portfolio_style--1 mt--30 card h-100">
                            <div class="thumb card-body">
                                <h3 class="px-3">{{title}}</h3>
                                <img class="adoption-image" src="{{file}}" alt="No se puede visualizar la imagen">
                                <p class="pt-4 dog-description">🧡 {{description}}</p>
                                <p>{{title}} está buscando: <strong> {{category.title}} </strong></p>
                                <a href="/pichicho/{{_id}}">
                                    <button class="btn btn-yellow d-lg-none text-center w-100 text-white">
                                       <strong> Ver más sobre {{title}} </strong>
                                    </button>
                                </a>
                            </div>
                            
                            <a href="/pichicho/{{_id}}" class="d-none d-sm-block">
                            <div class="port-overlay-info">
                                
                                <div class="hover-action">
                                    <h3 class="post-overlay-title py-5">
                                        Ver más sobre {{title}}
                                    </h3>
                                </div>
                            </div>
                            </a>
                        
                        </div>
                    </div>
                    <!-- End Single Portfolio -->
                    {{/each}}
                </div>
            </div>
        </div>
        <!-- End Portfolio Area -->

        {{/if}}
        <br>
        <br>
    </main>
    <!--// Page Conttent -->
</div>